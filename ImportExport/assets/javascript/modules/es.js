/*jshint esversion: 6 */
export default function (foo, bar) {
	return foo + " " + bar;
}

export function A(foo, bar) {
	return foo + " " + bar;
}

export function B(foo, bar) {
	return foo + " " + bar;
}